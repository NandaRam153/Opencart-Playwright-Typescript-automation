import { test } from '@playwright/test';

/**
 * Scaffolding seed for playwright-test-generator agents.
 * Excluded from test runs via testIgnore in playwright.config.ts.
 *
 * Showcase output: src/tests/integration/TabletsCategory.spec.ts
 * See docs/test-generation-from-seed.md and specs/test.plan.md (Tablets showcase).
 */
test.describe('Seed', () => {
    test('seed', async () => {
        // generate code here
    });
});
